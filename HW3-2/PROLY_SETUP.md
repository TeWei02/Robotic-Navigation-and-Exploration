# Proly Setup

`Proly.app` is not committed to this repository because the app bundle is very large.

Use this command in `HW3-2/`:

```bash
chmod +x get_proly.sh
./get_proly.sh
```

After download and unzip, `Proly.app` will exist in the same folder.
